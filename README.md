A quick demo for Juno
A new landing page design

To view the website live, you may visit
https://jacobjonesdev.github.io/

To run the site locally, simply run:
'npm install'
in the root directory, and then run
'npm start'
The site should now be viewable at 'localhost:3000'
